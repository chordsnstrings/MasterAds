{
  "name": "@engine/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "migrate": "node --import tsx src/migrate.ts",
    "seed": "node --import tsx src/seed.ts",
    "kill-switch": "node --import tsx src/kill-switch-cli.ts"
  },
  "dependencies": {
    "@engine/contracts": "workspace:*",
    "drizzle-orm": "^0.36.0",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^22.19.20",
    "@types/pg": "^8.11.10",
    "drizzle-kit": "^0.28.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
