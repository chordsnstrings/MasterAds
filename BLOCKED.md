# BLOCKED.md

Human-supplied prerequisites the build cannot create. Each entry: what is needed,
where it plugs in, and what runs in stub mode meanwhile. The build never blocks on these.

(Entries accumulate as chunks reach their live-mode boundaries; finalized at G13.)
